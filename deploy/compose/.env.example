# Generate with: docker run --rm ghcr.io/kurotch-homelab/smtp-auth-proxy:latest genkey -quiet
# This key decrypts every stored OAuth secret. Losing it means re-entering
# them; leaking it means they are not protected.
SMTP_AUTH_PROXY_ENCRYPTION_KEY=

# Only used with the PostgreSQL overlay.
POSTGRES_PASSWORD=
